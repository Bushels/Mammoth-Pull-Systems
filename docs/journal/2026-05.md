# May 2026 Journal

## 2026-05-01 — Portfolio Cleanup

Ran the MPS portfolio cleanup workflow (`docs/templates/portfolio-cleanup-prompt.md`) against `C:/Users/kyle/MPS/Mammoth Pull Systems`. Discovery and gitignore audit performed by Claude (Opus 4.7); execution dispatched to Codex (GPT-5.5) via `codex:rescue`; outside-sandbox finishing touches by Claude.

### Verified state

Clean tree at `8990ba6 feat(seo): add Microsoft Clarity, FAQ schema, and tighten sitemap`. Next.js 16.2.4, React 19.2.4, Tailwind 4, Resend, Vercel analytics. `AGENTS.md` is already rules-only (5-line Next.js breaking-changes note); `CLAUDE.md` is already `@AGENTS.md`; no `GEMINI.md`; no stale model strings (`claude-opus-4-*`, `gpt-5-4`) anywhere in tracked source.

### Changes applied

- `.gitignore` — replaced the bare `.claude/` ignore with a sandwich (`.claude/*` + `!.claude/agents/**` + `!.claude/skills/**`) and an inline comment explaining intent. Runtime files (`scheduled_tasks.lock`, `settings.local.json`) remain ignored. Verified with `git check-ignore -v`.
- `.claude/agents/.gitkeep` — created (placeholder; no project-specific agents yet).
- `.claude/skills/.gitkeep` — created.
- `.claude/skills/next-16-conventions/SKILL.md` — **first project-specific skill**: surfaces the AGENTS.md "Next.js 16 has breaking changes" rule with prompt signals so it auto-activates whenever Claude or Codex is about to touch `src/app/**`, `src/components/**`, `next.config.ts`, or any related prompt. Pins the verified stack versions and lists the common drift traps (Pages Router idioms, Edge-only middleware, etc.).
- `.vscode/launch.json` — moved from the misplaced `.claude/launch.json` via shell move (source was untracked/ignored).
- `.codex/config.toml` — created with `model = "gpt-5.5"` and `model_reasoning_effort = "medium"` per the cleanup prompt's current SOTA defaults. **First draft used a `[model]` TOML table** with nested `name`/`reasoning_effort` keys; Codex's review pass caught that the CLI expects flat top-level keys (`model = "..."`, `model_reasoning_effort = "..."`) and would have crashed any new session. Fixed before commit. Lesson: a Codex fresh-eyes review at the end of a Claude-led cleanup catches model/parser drift that's invisible from the writer's seat.
- `mammoth-site-concept.md` → `docs/concepts/mammoth-site-concept.md` — relocated via `git mv` (history preserved). The repo root no longer carries a floating concept doc.
- `PROJECT_STATE.md` — created at repo root with last verified commit, blockers, next actions, and a "where to find what" map.
- `docs/journal/2026-05.md` — this entry, written last.

### Sandbox blockers (resolved)

Codex's sandboxed user `Bushels\CodexSandboxUsers` had explicit deny-ACLs on `.git/` and `.codex/`, which:
- Prevented Codex from writing `.codex/config.toml` itself — finished by Claude (`BUSHELS\kyle` has full control).
- Prevented Codex from running `git add`/`commit` — which is fine, the cleanup prompt explicitly says "Do not commit until I approve the diff."

### Deletion candidates — proposed only, deferred for owner decision

| Path | Size | Rationale |
|---|---|---|
| `.next/` | varies | Next.js build cache, regenerated on every build |
| `.playwright-cli/` | 120 K | Old Playwright CLI run logs (April 16) |
| `.playwright-mcp/` | 290 K | Old Playwright MCP run logs (April 16) |
| `.dev-server.log` / `.dev-server.err.log` | 24 K | Stale dev-server logs (April 16) |
| `output/` | 8.3 M | Stale generated output (`output/playwright/`) |
| `tsconfig.tsbuildinfo` | 106 K | TS incremental cache, regenerated |
| `Images_Videos/` | **19 M** | Raw HEIC/JPG iOS dump. Filenames share **zero overlap** with the 19 curated names in `public/media/` (e.g. `20260319_134043000_iOS.jpg` vs `hero-convoy.jpg`), so the only way to verify every needed asset is already curated is a visual pass. Recommend keeping until owner confirms. |

### Next actions

Owner to: (1) review the diff, (2) `git add` + commit the cleanup, (3) decide on the deferred deletions.
